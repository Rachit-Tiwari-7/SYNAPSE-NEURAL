'use client';

import React from 'react';

/**
 * Fallback stub for AiHealthChatPanel.
 * Provides backwards-compatibility for cached browser sessions and Turbopack manifests.
 */
export default function AiHealthChatPanel(_props?: any) {
  return null;
}
